# Block web access to SQLite databases, CSVs, and environment files
<FilesMatch "\.(sqlite|csv|sql|env)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</FilesMatch>